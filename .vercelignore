# Vercel only hosts the console's demo build (see vercel.json); everything
# else stays out of the deployment. The repository deliberately has no
# top-level api/ directory: Vercel would try to build it as functions.
/*
!/console
!/vercel.json
/console/node_modules
