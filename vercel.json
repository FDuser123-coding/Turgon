{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "cd console && npm ci --no-audit --no-fund",
  "buildCommand": "cd console && npm run build",
  "outputDirectory": "console/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
